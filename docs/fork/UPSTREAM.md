# Upstream provenance

Official repository: https://github.com/tinyhumansai/medulla

Documentation baseline: `b0c4f442ca00797dbf07338a52bad7d2cc382827` (`main`).

Source reviewed: `714c05c1d3f24dbddbfc4d298305244706055254` on `subconscious-graph-redesign`, August 27, 2026, manifest version0.11.0 and license declaration GPL-3.0-only. Source components inspected: runtime contract, provider selection, workflow run finalization/cancellation, run registry, and protocol identity.

The separate linked `tinyhumansai/medulla-src` returned404 during evaluation. The retained public branch makes historical source review possible; it does not establish current-release equivalence. Its OpenHuman submodule `e1c332bf08b332dacc5dcdb04c1d8df6f8401e13` was not initialized or executed.

The fork's `orchestrator/` is new local code. Upstream Rust source, installers and cloud dependencies are not required to run it. Historical upstream documentation describes upstream behavior and is not the local runtime's contract.
